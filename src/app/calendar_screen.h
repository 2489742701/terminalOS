#pragma once
#include <lvgl.h>

/* ═══════════════════════════════════════════════════════════════════════════
 * 日历（公历月视图）
 *
 * master 2026-09-25：「我们忘记日期了，加进去。日历系统占用高吗？」
 *   —— **不高**，而且是按需创建（跟别的应用一样，退出就销毁）：
 *      · Flash：约 3 KB（纯代码，没有农历 / 节假日数据表）
 *      · 运行 DRAM：约 10 KB（42 个日期格 + 表头，屏在才占）
 *      · 常驻 DRAM：0（不发任务、不建定时器）
 *   想加农历 / 节假日才会变贵：光农历闰月表就要 ~10 KB Flash，
 *   真要做再说，别提前背这个重量。
 *
 * 交互：
 *   · 顶部「< 2026年9月 >」翻月
 *   · 今天高亮（金色边框 + 金数字）
 *   · 点日期格 -> 底部显示那天的星期（Zeller 公式，跟天气页同一套）
 *   · 左右滑退出（统一手势）；竖滑翻月
 * ═══════════════════════════════════════════════════════════════════════════ */

lv_obj_t* CalendarScreen_create();
void CalendarScreen_tick();
