#pragma once
#include <lvgl.h>

/* ═══════════════════════════════════════════════════════════════════════════
 * 后台管理（任务管理器）
 *
 * master 2026-09-24 定的产品语义：
 *   应用**不是常驻**的 —— 按需启动（nav_open），退出/抢占时销毁（nav_release_*）。
 *   但"退出"只是销毁对象树，某些应用在切换过程中仍留在内存里，
 *   所以需要一个后台页把它们列出来、让用户能逐个关掉。
 *
 * 数据来自 nav.cpp 的 Activity 表：谁在内存里、创建时吃掉多少内部 DRAM。
 * ⚠️ 前台应用（当前这一屏）和 Launcher 不允许关 —— 删当前屏 = 删掉 LVGL
 *    正在渲染的树，必崩（项目里崩过多次）。按钮显示为「前台」而不是置灰隐藏，
 *    免得用户以为没这个应用。
 * ═══════════════════════════════════════════════════════════════════════════ */

lv_obj_t* TaskMgrScreen_create();

// 每 2 秒刷一次内存数字（跟 sysinfo 一样的节奏）
void TaskMgrScreen_tick();
