# AGENT.md · 本项目的协作契约

> 给在这个仓库里干活的 AI / 协作者。**开工前读完本文 + [`docs/00`](docs/00-项目上手指南.md)。**
> 这里写的是"怎么干活"，具体技术坑在 [`docs/14` 坑点速查表](docs/09-坑点速查表.md)。

---

## 1. 这个项目的基本事实

ESP32-S3 + 480×480 RGB 并行屏的口袋终端，核心是**自带浏览器**。
PlatformIO + Arduino + LVGL 8.3 + Lexbor，Windows 主机，串口 COM7。

**它是嵌入式项目，不是 Web 项目。** 一次"改代码 → 编译 → 烧录 → 上板验证"要几分钟，
没有热更新、没有单元测试兜底、崩了就是重启。这决定了下面的所有规矩。

---

## 2. 工作方式（master 的偏好，务必遵守）

### 2.1 先挖根因，再动手修
看到现象**先取证**，不要急着改代码。

> "先写出来再找问题" 适用于**交付**；排查阶段必须反过来 —— 先搞清楚是什么，再谈怎么改。

**取证手段**（按成本排序）：
1. 抓设备串口日志（`reset_capture.py` / `serial_cmd.py`）
2. 把设备收到的**原始 HTML** 拉到 PC 对比（`dl` + `serve`，或 `fetch_dev_html.py`）
3. PC 上用 raw socket 复现设备请求（改 UA / header 逐项对照）
4. 崩溃 → `tools/a2l.py <PC>` 做 addr2line
5. 编译没生效 → 对比 `.o` 与 `.cpp` 的 mtime

**判定"根因找到了"的标准**：能解释**所有**观察到的现象，包括反常的那些。
解释不了的就继续挖，别拿"大概是因为…"交付。

### 2.2 汇报要高信号
说**根因、影响面、待决策项**。不复述过程命令，不罗列"我做了 1、2、3"。

坏例子：
> 我改了 layout_engine.cpp，加了 flat_is_serp_chrome_link 函数，然后重新编译，
> 用 flash_run.py 烧录到 COM7，然后运行 probe_bing10.py…

好例子：
> 必应对无 JS 客户端按 UA 降级：移动 UA 只给 5 条结果且无分页，桌面 UA 给 10 条 + 下一页。
> 已改必应走桌面 UA。实测 5→10 条，垃圾过滤 5→18 条。**待你拍板：「下一页」点了内容不变，留不留？**

### 2.3 YAGNI
等**第二个**用例出现再抽象共享代码。不提前设计、不提前分层、不写"以后可能会用到"的东西。

### 2.4 破坏性操作先列清单等确认
删大文件、批量改文件名、改产品语义（比如改按钮行为）、动 `platformio.ini` 的板级配置
—— 都**先列清单，等 master 明确确认**再执行。

### 2.5 master 会主动纠正
他会修正过时的信息、纠正改错的东西。**不要拿训练数据当现状**（年份、仓库状态、库版本）。
他会一次性提多个混合问题（视觉迭代型），逐条回应，别漏。

---

## 3. 本项目专属红线

### 3.1 编译后必须验证"真的编进去了"
**对比 `.o` 与 `.cpp` 的 mtime**，`obj` ≥ `src` 才算数。
退出码 0 和日志行数**都不可信** —— 本项目踩过多次 PIO 静默跳过编译，烧的还是旧固件。

```python
# 判据：os.path.getmtime(obj) >= os.path.getmtime(src)
```
日志文件名别猜：`build_run.py` → `build_sdkdef.log`，`flash_run.py` → `flash_sdkdef.log`。

### 3.2 改完必须上板验证
"编译通过" ≠ "生效" ≠ "对了"。改浏览器/排版/UA 这类，**必须跑一次设备探针**看真实输出。

### 3.3 ⚠️ Edit 工具会报成功但不落盘
本项目的 **CRLF 文件上已踩 6+ 次**。
**每次编辑后必须回读核对**（grep 那一行）。不确定就用托管 python 做字符串替换 + 立即校验。
修 C++ 源码时，本项目已有一堆 `tools/fix_*.py` 一次性补丁脚本，照着那个模式写。

### 3.4 别用裸 pio / 裸 esptool
走 `tools/build_run.py` 和 `tools/flash_run.py`——它们设好了 safe-delete 阈值和 `--flash_mode dio`。

### 3.5 bash shim 是坏的
`ls`/`head`/`grep`/`tail`/`dirname` 都不存在，`rc=127` 不代表命令失败。
用 PowerShell 或托管 python。⚠️ `cmd | tail -40` 会因为 `tail` 不存在而整体失败。

解释器：
- **platformio 只在** `C:\Users\longyaosi\python-sdk\python3.13.2\python.exe`
- 托管 python（3.13.12）用来跑脚本，没装 platformio

---

## 4. 排查套路（本项目已验证有效）

| 症状 | 第一步 |
|---|---|
| 开不了机 / 重启 | 看 `Saved PC` 是否**恒定**：恒定 = 主动死循环；变动 = 真崩溃 |
| 崩溃 | `a2l.py <PC>`；**回溯里第一个属于 `src/` 的帧才是真凶**（崩在 LVGL 里 ≠ LVGL 的错） |
| 二分定位 | `tools/minimal_run.py` 极简工程；它也挂 ⇒ 配置问题 |
| 内存见底 | `heap_caps_get_free_size(MALLOC_CAP_INTERNAL)`；🚫 别用 `xPortGetFreeHeapSize()`（含 PSRAM） |
| 页面元素消失/排版乱 | `dl` + `serve` 把原始 HTML 拉到 PC 上对比，先确认**服务端给了什么** |
| 渲染"少了东西" | 先 `flatdump 200` 再刷新 —— dump 默认只打 60 行，会让你误判 |
| 网络结果不对 | PC 上 raw socket 复现，逐项对照 UA / header / 是否 302 |

---

## 5. 知识落盘（改完代码必做）

**排查出新坑 = 必须写文档。** 不然下次还要重新挖一遍。

| 情况 | 写到哪 |
|---|---|
| 新的坑 / 新的"别这么做" | [`docs/14` 坑点速查表](docs/09-坑点速查表.md)（按症状分类，加一行） |
| 涉及某个专题的原理 | 对应的 `docs/01`～`08` |
| 新增串口命令 / 工具脚本 | [`docs/00` §4 / §5](docs/00-项目上手指南.md) |
| 结论速记（跨会话复用） | [`docs/10` 项目长期记忆](docs/10-项目长期记忆.md) |
| 项目结论（会跨会话复用的） | `.workbuddy/memory/MEMORY.md` |
| 当天做了什么 | `.workbuddy/memory/YYYY-MM-DD.md`（append-only） |

文档风格：**结论 + 指针**，不写流水账。同类知识优先更新已有文档，不要新建重复的。

---

## 6. 我（小buddy）在这个项目上踩过的错

写下来是为了**不再犯第二次**：

1. **把「三个点（更多）」改成了「回到首页」** —— 没搞清产品语义就动手。
   三个点 = 展开底部搜索框 + 刷新 + 退出浏览器；首页图标 = 去我们自己的搜索首页。
   **教训：改 UI 语义前先确认它现在是什么行为。**
2. **误判"下一页没渲染"** —— 其实是诊断 dump 被 60 行截断，渲染是好的。
   **教训：排查工具本身也会骗人，先怀疑工具。**
3. **过滤规则按 href 匹配导致连坐** —— 容器 `<li>` 会继承子树 href，
   按 `first=` 过滤把整棵子树（含「下一页」）一起删了。
   **教训：作用在容器上的规则要限定到叶子节点。**
4. **拿 UA 理论硬套** —— 以为换 Android13 就有分页，实测是桌面 UA 才有。
   **教训：先做矩阵实验（域名 × UA），别单点猜。**
5. **Edit 静默失败** —— 报成功没落盘，浪费了好几轮。见 §3.3。

---

## 7. 一句话速查

- 开不了机 → [`09` A 段](docs/09-坑点速查表.md)
- 想改排版 → 先看 [`04`](docs/04-浏览器排版与渲染.md) 和 [`09` F 段](docs/09-坑点速查表.md)
- 想改字体 → **先读 [`06`](docs/06-中文字体.md)**
- 想改构建配置 → **先读 [`01`](docs/01-板级硬事实与构建烧录.md)**，那几条改错开不了机
- 搜索/UA/分页 → [`05`](docs/05-搜索与SERP.md)
