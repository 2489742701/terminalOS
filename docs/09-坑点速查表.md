# 09 · 坑点速查表

> **按「你看到的现象」反查。** 每条给：症状 → 根因 → 动作 → 详见哪篇。
> 深度原理看对应编号文档，本文只负责让你在 30 秒内定位。
>
> 排序：**A 开不了机 → B 显示 → C 崩溃 → D 内存 → E 构建环境 → F 排版 → G 字体 → H 网络 → I 生命周期 → J 工具**

---

## A. 开不了机 / 无限重启

### A1 · `rst:0x3` 无限重启，**没有任何 panic 输出**
- **根因**：不是崩溃，是**主动死循环**。多半是分区表越界 → bootloader 进 `while(1)` → 被 RTC 看门狗复位。
- **动作**：⛔ 立即检查有没有配 `board_build.flash_size = 16MB` 或 `partitions_16MB.csv`。板子物理是 16MB，但**本工程编译出的 bootloader 只认 8MB**。删掉，用默认 8MB 分区。
- **另查**：`Saved PC` 是否恒定。恒定 = 死循环；每次不同 = 真崩溃。
- **详见**：[`01` §1.4](01-板级硬事实与构建烧录.md) / [`07`](07-排障方法论.md)

### A2 · 卡在 bootloader，每秒一次 TG0WDT 复位
- **根因**：Flash 模式配成 QIO。这块板必须 **DIO**。
- **动作**：`board_build.flash_mode = dio`；**裸 esptool 直烧也必须带 `--flash_mode dio`**。
- **详见**：[`01` §1](01-板级硬事实与构建烧录.md)

### A3 · PSRAM 相关异常 / 缓冲没落在 PSRAM
- **根因**：PSRAM 线模式。这块板是 **OPI**。
- **动作**：`board_build.arduino.memory_type = qio_opi`。
- ⚠️ **DIO 与 OPI 必须同时成立**，缺一开不了机。
- **成功标志**：`ptr=0x3d8712dc`（`0x3d…` = PSRAM 段）。
- **详见**：[`01` §1](01-板级硬事实与构建烧录.md)

### A4 · 不知道是崩溃还是死循环
- **动作**：用极简工程二分 —— `tools/minimal_run.py`。连极简工程也挂 ⇒ 是配置问题，不是你的代码。
- **详见**：[`07`](07-排障方法论.md)

---

## B. 显示异常

### B1 · 刷新瞬间花屏 / 撕裂 / 噪点
- **根因**：**带宽争抢** —— 帧缓冲在 PSRAM，RGB 并行 + GDMA 靠 bounce buffer（须内部 DMA 内存）中转，PSRAM OPI 80MHz 与片内 flash 同一条总线分时复用。
- **已定的解**：`PCLK = 10MHz`（**别改回 16**）；LVGL 缓冲 PSRAM 双缓冲 × 120 行。
- **分水岭判据**：静态正常、只刷新时乱 → 带宽争抢；**静态也花** → 时序错（去查 ST7701S 时序）。
- **详见**：[`02`](02-显示-花屏与ST7701S.md)

### B2 · ST7701S 配置别动
`st7701_type1_init_operations` + RGB 时序 H 10/8/50、V 10/8/20、IPS/BGR=true。这几个值都是试出来的，**不要"顺手优化"**。

### B3 · 上滑时左下角一条白线
**2026-09-24 定位 + 修 + 上板确认已消失**：LVGL 的滚动条。
`lv_obj` 默认 `LV_SCROLLBAR_MODE_AUTO`（`lv_obj.c:348`），可滚动时沿边沿画一条
滚动条；本工程没给 `LV_PART_SCROLLBAR` 设自定义样式，用的是默认浅色 ——
在黑底 UI 上就是一条白线，且只在"内容比容器长"时出现，跟"上滑"高度相关。

- `desktop_screen.cpp` 早就 `lv_obj_set_scrollbar_mode(cont, LV_SCROLLBAR_MODE_OFF)`，
  `browser_screen.cpp` 的 `g_content` **漏了** → 已补。
- **自查方法**：任何可滚动容器都要显式设 `LV_SCROLLBAR_MODE_OFF`（或给 SCROLLBAR
  设一套暗色样式）。`grep -rn "scroll_dir" src/` 找出所有可滚动对象逐个核对。

### B3b · 触摸坐标偏移（2026-09-24 定位）
根因有两个，都在 `src/hal/touch.cpp` 的映射表达式里：

1. **翻转了两次**：TAMC_GT911 库 `setRotation(ROTATION_NORMAL)` 已经做过
   `x = width - x`，旧代码又 `map(p.x, 480, 0, 0, 479)` 翻一次 —— 两者对消，
   实际等价于"裸值 ×479/480"。看代码像是翻转，其实没有。
2. **端点写死 480**：若 GT911 配置寄存器里的 `X/Y_OUTPUT_MAX` 不是 480，
   裸值根本不落在 0..480，于是整屏偏移/缩放都不对。

**已改**：两点线性映射 + 三个串口命令 ——
`tprobe`（读 GT911 的 X/Y_OUTPUT_MAX，不是 480 就是根因 2）、
`traw [s]`（10s 内连续打裸坐标，逐个点四角）、
`tcal x0 x1 y0 y1`（填实测端点，立即生效，不用重烧）、
`tswap [on|off]`（X/Y 交换，面板贴反时试它）。
默认值 0/480/0/480 只为了复刻旧行为，**不是真值**。

2026-09-24 实测 `tprobe`：**X/Y_OUTPUT_MAX 都是 480，与屏幕一致** ——
根因 2 排除。那么只剩三种可能：轴向翻转（用 `tcal 480 0 480 0` 试）、
X-Y 交换（用 `tswap` 试）、常量偏移（用 `traw` 量四角后填端点）。

### B4 · IDF 4.4 升不了级
别浪费时间试，此路不通。

---

## C. 崩溃（LoadProhibited / StoreProhibited 等）

### C1 · 怎么定位
```bash
"C:/Users/longyaosi/.workbuddy/binaries/python/versions/3.13.12/python.exe" tools/a2l.py <PC>
# 或裸用：
~/.platformio/packages/toolchain-xtensa-esp32s3/bin/xtensa-esp32s3-elf-addr2line.exe \
  -e .wb_build/esp32s3/firmware.elf -f -C <PC>
```

### C2 · ⚠️ 崩在 LVGL 里 ≠ LVGL 的错
回溯里**第一个属于 `src/` 的帧**才是真凶。LVGL/lexbor 只是被喂了坏指针。

### C3 · 渲染成功、过 1 秒才崩
- **根因**：常驻 `lv_timer` 持有已销毁对象的指针。`lv_timer` **不属于对象树**，不会随对象自动注销。
- **动作**：持有对象指针的定时器必须挂 `LV_EVENT_DELETE` 注销，或每拍验活。
- **详见**：[`07`](07-排障方法论.md)

### C4 · 开机必崩（随包 LVGL 的 bug）
vendored `lv_draw_sw_arc.c` 有 bug：部分圆弧分支无条件 `free` 未初始化的 `mask_in_param`
→ LoadProhibited。**已补守卫**。另：`icons.cpp::drawWifi()` 改用折线，不碰 `lv_draw_arc`。

### C5 · 退出/切屏时崩
- **根因**：先切屏再删屏、或在对象回调里删它所在的屏。
- **动作**：🚫 **先切屏再删屏**；🚫 别在对象回调里删它所在的屏。
- ⚠️ `lv_scr_load_anim(..., auto_del=true)` 删的是**被替换掉的旧屏**，不是你想删的那屏。
- **详见**：[`03`](03-浏览器架构与生命周期.md) / [`07`](07-排障方法论.md)

---

### C6 · `lv_obj_remove_style_all()` 触发 panic
- **症状**：`Cache disabled but cached memory region accessed`
- **动作**：别用它。改成逐项清零 —— `lv_obj_set_style_border_width(obj,0,0)` +
  `lv_obj_set_style_pad_all(obj,0,0)` + `lv_obj_set_style_bg_opa(...)` +
  `lv_obj_clear_flag(LV_OBJ_FLAG_SCROLLABLE)`。

### C7 · Canvas 缓冲越界（画出来一条白线）
- **根因**：`LV_IMG_CF_TRUE_COLOR_ALPHA` 是 **4 B/px**，`size*size*3` 分配会越界写。
- **动作**：改成 `size*size*4`，并从 PSRAM 分配（`heap_caps_malloc(MALLOC_CAP_SPIRAM)`）。

### C8 · ⚠️ `A stack overflow in task loopTask has been detected`（2026-09-23 实测）

**症状**（极易误判成"内存不够"）：
- 打开某个网页时整机重启
- **画面还停着，但触摸完全失效** —— RGB 并行屏由 GDMA 自行刷新，
  主任务已经死了，DMA 还在刷最后一帧
- 串口在重启前会明确写：
  `***ERROR*** A stack overflow in task loopTask has been detected.`
- `Backtrace` 结尾常带 `|<-CORRUPTED`（栈被打烂，回溯不可信）
- **跟内存无关** —— 实测出事时 DRAM 还剩 203KB

**根因：是栈不是堆。**
Arduino-ESP32 的 `loopTask` 栈默认只有 **8192 B**。而 layout_engine 里有 4 个
全树遍历函数把 `next_sibling` 也写成了递归：

```c
layout_tree_stats        / layout_flatten_tree
layout_scale_tree        / layout_clamp_horizontal
    ...
    f(node->first_child, ...);
    f(node->next_sibling, ...);   /* ← 凶手 */
```

于是递归深度 = **树的节点总数**（几百到上千），而不是嵌套层数。
必应搜索页 356 个节点侥幸过关，乐鑫官网 977 个节点直接炸。

**怎么判别**：报错里写的是 `task loopTask` = UI 主任务；
若后台任务崩，写的是 `task browser_fetch`（那是另一回事，见 D4）。

**修复（已在 2026-09-23 做完）**：
- 上面 4 个函数 sibling 改迭代：`while (node) { ...; node = node->next_sibling; }`
- `platformio.ini` 加 `-DARDUINO_LOOP_STACK_SIZE=32768`（8KB → 32KB）
- 剩下纯父子递归的 `layout_render_node` / `layout_drop_junk` 加 `MAX_LAYOUT_DEPTH 64` 上限
- ⛔ **`layout_node_destroy` 绝不能加深** —— 中途截断 = 内存泄漏
- 加了永续预警：每次渲染完串口打
  `[Browser] loopTask stack: xxxx B left (peak used xxxx B)`
  低于 ~8KB 就该回头查递归是不是又失控了

**修复后的实测对照**：同一条三级跳转路径，977 节点的页面从「必崩」变成
正常渲染，栈峰值 11108 B、剩余 21660 B。

---
## D. 内存

### D1 · 为什么 PSRAM 空着 7MB、DRAM 却见底
- **根因**：`CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=4096` —— **≤4KB 的 malloc 一律进内部 DRAM**。
  布局树节点、`text_content`、href/class/style 拷贝全是小分配 ⇒ DRAM 被一点点榨干。
- **已定的解**：解析阶段改用 `tb_alloc` / `tb_calloc`（PSRAM 优先，malloc 兜底），
  `src/browser_engine/src/tactilebrowser_core.cpp`。实测 DRAM 开销 40KB → 4KB。
- **详见**：[`03`](03-浏览器架构与生命周期.md)

### D2 · 量化内存用错 API
- ✅ `heap_caps_get_free_size(MALLOC_CAP_INTERNAL)` —— 这才是 DRAM
- 🚫 `xPortGetFreeHeapSize()` —— **含 PSRAM**，别当 DRAM 用

### D3 · 🚫 运行时禁调这两个
```c
heap_caps_get_largest_free_block();  // O(n) 遍历整堆持锁
heap_caps_get_info();                // 同上
```
撞上 WiFi 收包直接 **wdt 重启**。addr2line 实证过。
- **详见**：[`03` §6.2](03-浏览器架构与生命周期.md)

### D4 · 任务栈只从内部 DRAM 分配
→ 后台任务要**一次性常驻复用**（notify 唤醒），**别反复 create/delete**。
fetch 任务栈 16384（实测峰值 3712 B）。

### D5 · 无 MMU 的现实
不 `lv_obj_del()` 就永不释放。LVGL 池**已在 PSRAM**（`LV_MEM_POOL_IN_PSRAM 1`，
在 `src/config/lv_conf.h`）。开机内部 DRAM free 250KB，PSRAM 7.2MB。
别想"运行时搬来搬去"——两段物理地址，搬运 = 手写 GC。

### D6 · chunked 读取写穿堆
HTTPS 下载上限 786432 B。chunked 读取**必须钳 `room`/`want`**，
否则写穿 PSRAM 堆，表现为 WiFi 驱动报 `block_trim_free heap_tlsf.c:371`（看着像 WiFi 的锅）。

---

## E. 构建 / 烧录 / 环境

### E1 · ⚠️ 编译返回 0 但固件没更新（本项目最高频陷阱）
- **判据**：**对比 `.o` 与 `.cpp` 的 mtime**，`obj` 必须 ≥ `src`。退出码和日志行数都不可信。
- **日志文件名别猜**：`build_run.py` → `build_sdkdef.log`，`flash_run.py` → `flash_sdkdef.log`。
  查错日志会误判成"静默跳过编译"。
- **详见**：[`01` §6 / §6.1](01-板级硬事实与构建烧录.md)

### E2 · 日志停在 "Took 17.81 seconds" + [FAILED]，看着像编译错误
- **根因**：safe-delete 按「单进程内累计删除数」计 50/轮，SCons 全量重编会撞上。
- **已定的解**：`build_run.py` / `flash_run.py` 已设 `CODEBUDDY_SAFE_DELETE_BULK_THRESHOLD=100000`。
  **别用裸 pio 命令绕开这两个脚本。**

### E3 · ⛔ 别删 `.pio`
会逼重下依赖库 + 被 safe-delete 拦。要重编只删 `.wb_build`。

### E4 · platformio 只在指定解释器里
`C:\Users\longyaosi\python-sdk\python3.13.2\python.exe`。托管 python 3.13.12 **没有** platformio。

### E5 · 改 vendored 库改哪里
**LVGL 源码在** `../4.0inch_ESP32-4848S040/1-Demo/Demo_Arduino/Libraries/Lvgl`
（不是 `.pio`，也不是本工程的 `Libraries/`）。lexbor 在 `src/browser_engine/lexbor/`。

### E6 · 构建期两个既有绕法（别"清理"掉）
1. Windows 大小写不敏感：`#include <time.h>` 会被 `Libraries/Time/Time.h` 遮蔽
   → `platformio.ini` 里有 `lib_ignore = Time`。
2. time API 需要 `-D_GNU_SOURCE`（只给 `_POSIX_C_SOURCE` 会缺 `settimeofday`）。

### E7 · CH340 掉线（Error 31 / ports 变 0）
先**软件复位**：PowerShell `Disable-PnpDevice` / `Enable-PnpDevice`。
真掉线才拔线。查物理连接：`Get-PnpDevice -PresentOnly`。


### E8 · ⚠️ LVGL 配置有两份，改错那份不生效（2026-09-23 核实）
编译时真正生效的是 **vendored 库里那份**：
`../4.0inch_ESP32-4848S040/1-Demo/Demo_Arduino/Libraries/Lvgl/lv_conf.h`
（26972 B，`LV_MEM_POOL_IN_PSRAM 1`、`LV_MEM_SIZE (128U*1024U)`）

`src/config/lv_conf.h`（25690 B，`LV_MEM_SIZE (192U*1024U)`，**没有** `LV_MEM_POOL_IN_PSRAM` 宏）
**不会被编译进去** —— 改它等于白改，而且不会报任何错。

- **动作**：改 LVGL 行为（内存池、日志、widget 开关）一律改 `Libraries/Lvgl/lv_conf.h`。
- **判据**：改完编译，看设备开机时 LVGL 的 mem 打印是否跟着变。
- 同类提醒：改 lexbor 同理，改 `src/browser_engine/lexbor/`。

---

## F. 浏览器排版 / 渲染

### F1 · 布局压盖 / 元素零高 / 位置全乱
- **根因**：CSS 算出的 x/y/height 大量为 0，而 `create_label` 里是 `(void)x;(void)y`
  根本不消费坐标；唯一吃坐标的 `create_container` 就成了压盖/零高的源头。
- **已定的解**：**平铺模式** `layout_set_flat_mode(true)`（默认开，串口 `flat on|off`）——
  不建容器、满宽居左，症状全消。
- **路线**：A（还原 CSS 版面）已放弃，C（转码代理）不做，现走 **D 平铺 + 搜索优先**。
- **详见**：[`04`](04-浏览器排版与渲染.md)

### F2 · 行合并规则
- 无文字壳子透明钻入
- **跨壳（父不同）只放行链接**，普通文本不放行
- `<li><a>` 靠 `li_is_link_wrapper()` 保留内层 `<a>`

### F3 · 结构性容器被压成一行（如必应分页变成 `- 分页123下一页`）
- **根因**：`li_has_block_children()` 要求「≥2 个块级子元素」才拆开，
  但 `<li class="b_pag">` 只有**一个** `<nav>` 子元素，够不到门槛。
- **已定的解**：`nav / ul / ol / table` 这类**结构性容器出现一个就判块级**。
- **详见**：[`05` §10](05-搜索与SERP.md)

### F4 · 垃圾过滤 `layout_drop_junk()`
丢：正在刷新/加载/编译、加载更多、纯分隔符、`loading` 等。
必应 SERP 另丢：时间筛选 `filters=ex1`、全部/时间不限 `qpvt=`、顶部导航 `FORM=HDRSC`、
垂搜路径、纯数字页码。`FORM=PORE`（下一页）**反向保留**。
⚠️ 过滤规则要**只作用在真正的 `<a>` 上**——容器 `<li>` 会继承子树 href，
按 href 过滤会连坐删掉整棵子树（这个坑让「下一页」消失过一次）。

### F5 · 页面元素莫名整棵消失
- **根因**（百度首页搜不到搜索框的真凶）：外层 `<span>` 的图标字体字符让整棵子树被丢弃。
- **取证三步法**见 [`04`](04-浏览器排版与渲染.md)。

### F6 · ⚠️ 诊断 dump 被 60 行截断，让你误判"没渲染"
`[Flat]` 日志默认只打 60 行，**渲染本身没截断**。
排查第 60 行之后的内容（比如「下一页」）先 `flatdump 200` 再刷新。

### F7 · 链接点不动 / 点了跳错
三个坑，都已修：
1. 🚫 **别在 CLICKED 回调里 `startFetch()`**（会 clean 掉正在回调的对象）→ 走 `g_linkPending` 延迟通道，下一 tick 才导航
2. **必须用绝对 URL**（`href_path` 是没 host 的纯路径）→ 统一 `flat_link_target()` 优先 `href_resolved`
3. **URL 必须 `strdup`**（布局树渲染完就 free）→ 靠 `LV_EVENT_DELETE` 回收
- 诊断看 `clickable links=M`。

### F8 · 平铺跳过外部 CSS
`layout_get_flat_mode()` 判定后跳过。cn.bing.com 原本要串行拉 30+ 个 CSS = 30 次 TLS ≈ 1 分钟。

### F9 · 整页缩放的 viewport ↔ 屏幕换算
见 [`08`](08-UI约定与产品语义.md)，有实测数据表。默认 `vp 0` = 自动读 `<meta viewport>`。

---

### F10 · ⚠️ 伪链接被当成真网页下载（`javascript:` 等）

**症状**：点网页上的空按钮 / 菜单项后，进度条走到 60~70% 停下或直接重启；
日志里出现 `read 500000+ bytes`、节点数暴涨到几千、DRAM 一次掉上百 KB。

**根因**：现代网站大量 href 根本不是真地址 ——
`javascript: void(0);`（纯 JS 空按钮，乐鑫官网菜单里一大堆）、
`mailto:` `tel:` `#anchor`。这些一旦走下载流程，服务器往往返回一个大号 404 页。

更难的是它们**常被拼上域名前缀**，变成：
```
https://www.espressif.com.cn/zh-hans/products/socs//javascript: void(0);
```
只看开头是不是 `javascript:` 拦不住，必须查**子串**。

**实测代价**（2026-09-23）：一次这种点击下载了 507440 B、解析出 4780 个节点、
DRAM 从 207KB 掉到 72KB。连点几下就该重启了。

**动作**：`isPseudoUrl()` 在两个入口拦 —— `link_click_cb` 和 `startFetch`。
判定 = 前缀命中伪协议列表（javascript:/mailto:/tel:/data:/about:/blob:/sms:/intent:/…）
**或** 子串含 `javascript:` / `void(0)` **或** 以 `#` 开头。

---
## G. 字体（改前必读 `09`）

### G1 · 分工
- **汉字**走自生成 `font_zh_16`（3785 字）
- **英文/数字/半角**走内置 montserrat，靠 `.fallback` 兜底
- ⚠️ **字体里故意不含 ASCII** —— 缺字表现是 LVGL 画方框（豆腐块）

### G2 · ⚠️ 没有 `lv_font_set_fallback()`，字体还是 `const`
→ fallback 只能改**生成产物**（已写进 `tools/gen_font.js` 后处理）。
**重新生成后务必确认 `.fallback` 还在。**

### G3 · ⚠️ 查字体有哪些字别直接读 `unicode_list`
SPARSE_TINY 存的是**相对偏移**，绝对码位 = `range_start + unicode_list[n]`。

### G4 · 生成必须绕开 shell
字符集写 UTF-8 文件 → Node `spawnSync` 传 argv 数组。
工具链：`tools/fix_font_symbols.py` → `gen_fonts.py` → `font_glyphs.py`（查码点）。

### G5 · `lv_font_montserrat_16` 是 ASCII-only
别往里塞汉字，会变豆腐块。

### G6 · 历史坑
`font_zh_16` 曾缺全部 ASCII → 数字/英文全豆腐块。**符号表必须同时覆盖 CJK 和 ASCII。**

---

## H. 网络

### H1 · UA 决定一切（必应血案）
`www.bing.com` 会**地域 302 到 `cn.bing.com`**，然后按 UA 降级：

| UA | 体积 | 结果条数 | 分页 |
|---|---|---|---|
| 移动（KitKat / Android13 / iPhone） | 60KB | **5** | **无** |
| **桌面 Chrome120** | 100KB | **10** | 有「下一页」 |

- **已定的解**：**必应走桌面 Chrome120 UA**；百度/其它仍走 KitKat（避免 wappass 验证码）。
  在 `lvgl_renderer.cpp` 里按 host 分支。
- ⚠️ 桌面 UA 给百度会被 302 到 `wappass.baidu.com` 验证码页。

### H2 · 「下一页」是假分页
链接带 `FPIG=<32位hex>` token，但裸 `first=11`／带 FPIG／再加 cookie+Referer，
**三种方式与第 1 页相比新增结果都是 0**。必应对无 JS 客户端只下发固定约 10 条语料。
（`first=` 被服务端直接忽略。）

### H3 · 换引擎这条路是死的（都实测过）
| 引擎 | 结果 |
|---|---|
| DDG / Mojeek / searx / Brave | 此网络**全部超时** |
| 360 | 351KB，`pn=` 无效 |
| 搜狗 | 「下一页」是 JS 按钮 |
| 百度 `m.baidu.com/s?word=` | **2.93MB**（head 就 380KB），786KB 上限连正文都摸不到 |

### H4 · HTTPS 上限 786432 B
见 D6。

### H5 · NTP
`src/hal/ntp_time.cpp` 手写（🚫 勿用 NtpClientLib，会塞回 `Time` 库）。
⚠️ `ntp.aliyun.com` 间歇超时，**必须多服务器兜底**。

### H6 · HTTP 要跟 3xx
不跟的话 `www.baidu.com` 302→https 会被判成"网络错误"。已加 Location 递归（5 跳）。

### H7 · 内置页面服务器（下载后怎么看）
`dl` 存进 LittleFS → `serve` → PC 浏览器访问 `http://<板子IP>/` 拿原始 HTML。
⚠️ **修过的 bug**：`ls` 曾无条件 `LittleFS.end()`，会把正在跑的服务器搞成 404。
现在服务器在跑时不再卸载。

---

## I. LVGL 生命周期 / 状态栏

### I1 · 屏与定时器
见 C3 / C5。

### I2 · FPS 要数对
✅ 数 `disp->driver->monitor_cb`
🚫 数 `lv_timer_handler()` 只是主循环迭代数（虚高几百）

### I3 · 状态栏图标
- WiFi：RSSI → 格数；**未点亮格用 `0x555555` 暗色画出**（禁透明）
- 图标几何（2026-09-23 重画）：WiFi 扇心 `0.70·S`、三弧 222°→318°；蓝牙是 Feather 折线
- 电池外框独立 2~3px（旧版 `S/16` = 1px 看不见）
- 时间 12 小时制带 AM/PM

### I4 · LVGL 测量陷阱
`lv_obj_get_scroll_bottom()` 在控件刚创建后返回**陈旧坐标**。
必须先 `lv_obj_update_layout()`。

---

### I5 · ⚠️ 二级菜单切页：三条铁律（2026-09-24 设置页重构）
- **症状 A**：点列表行进子菜单，随机崩 / 崩在 `lv_obj_clean` 之后。
  **根因**：在行的 `LV_EVENT_CLICKED` 里 `lv_obj_clean()` 会删掉**正在分发事件的那一行**。
  **动作**：切页一律延迟一拍 —— 一次性 `lv_timer`（period=1, repeat=1）里再 clean + 重建。
- **症状 B**：列表右侧一条浅色竖条。**根因**：默认 `LV_SCROLLBAR_MODE_AUTO`。
  **动作**：可滚动容器显式 `LV_SCROLLBAR_MODE_OFF`（就是浏览器那条"白线"）。
- **症状 C**：`back` 点了什么也没发生，**而且不打日志**。
  **根因**：把**串口命令行缓冲区的指针**存进了页面栈 —— 命令一结束缓冲被下一条命令覆盖，
  `findPage()` 拿到的是 `"setpage back"` 而不是 `"browser"`，找不到页就静默 return。
  **动作**：栈里只存页表里的静态字面量（`settings_menu.cpp::canonPageId`）。
- **另**：栈约定 `s_stack[s_depth] == 当前页`（栈底是 root），back = `--depth` 再取。
  写成"先写后 ++"却"先 -- 后取"会取到自己（差一错误）。
- **另**：`settings.cpp` 的三组划分口径 = "改了它影响 **屏 / 浏览器 / 整机** 的哪一个"，
  加项时按这条归类，别按"看起来像不像"分组。
- **详见**：`src/app/settings_menu.cpp` / `settings.cpp` 头注释

### I6 · `lv_timer_get_user_data()` 在本工程 LVGL 里不存在
- **症状**：编译报 `was not declared in this scope`，编译器建议 `lv_anim_get_user_data`。
- **根因**：随板 BSP 的 LVGL 是 8.3.0-dev，只给了 `lv_timer_create(cb, period, user_data)`，
  没有配套的 getter。
- **动作**：在读侧直接取结构体字段 `t->user_data`（`lv_timer_t` 的成员是公开的）。
- **详见**：`src/app/status_bar.cpp::back_async_cb`

### I7 · ⚠️ 循环切换型选项：别把"永不 / 关闭"放进循环档（2026-09-24 事故）
- **症状**：设置里点一下「息屏超时」，设备**再也不息屏**，看着像坏了。
- **根因**：选项表是 `{30秒, 1分钟, 5分钟, 永不}`，而默认档正好是"5 分钟"（index 2）
  —— 用户点一下就跳到"永不"（index 3），一次点击就关掉了息屏保护。
- **动作**：这类会**关掉保护**的状态要么单独做一项，要么**要求二次确认**
  （本项目选了后者：切到「常亮」要 3 秒内再点一次）。息屏是省电 + 防烧屏，
  不该一次点击就能关。
- **另**：排查"怎么还不息屏"用串口 **`sleep`**（state/timeout/已空闲/suppressed）。
  ⚠️ 别忽略**两段式**：ACTIVE -> DIM(背光 15%，暗显时钟) -> 再 100s -> OFF(全黑)，
  中间那 100 秒看着像"没灭"。
- **另**：`valueFn` 遇到"当前值不在档位表"时要**如实显示**（如 0 → "永不"），
  别回退到默认档 —— 否则会出现"显示 5 分钟、实际永不"这种看不出来的不一致。
- **最终形态**：息屏超时改成了**滑块自由设定**（0~600 秒，0 = 常亮），循环档整个去掉
  —— 既满足"我想设 90 秒"，也不会一次点击就关掉保护。`vp` 排版视口仍是循环档（枚举值，无所谓）。
- **排版视口仍是循环档**（枚举值，无所谓）。
- **详见**：`src/app/settings.cpp` 里「息屏超时」上方的注释

### I8 · `Preferences::begin(ns, true)` 只读模式打开不存在的 namespace 会失败
- **症状**：`[E][Preferences.cpp:50] begin(): nvs_open failed: NOT_FOUND`，
  于是"设置持久化"**第一次开机必然失效**（还没写过的 namespace 根本不存在）。
- **根因**：Arduino-ESP32 的 Preferences 在**只读**模式下不会创建 namespace。
- **动作**：读写都用 `begin(ns, false)`（不存在则创建）。
- **另**：滑块拖动会连发几十次 `VALUE_CHANGED`，别每次都写 NVS —— 标脏 + 一次性
  lv_timer 延迟 800ms 合并落盘。
- **详见**：`src/app/settings_store.cpp`

### I9 · 滑动返回：必须是「左边缘起手 + 往里滑」，全屏横滑一律不算
- **背景**：以前每个屏自己调 `swipe_detect`，参数五花八门 —— 有的限制左边缘
  40px，有的**全屏任意位置横滑都能返回**，结果跟横向滚动 / 游戏操作打架。
- **约定**（master 2026-09-25）：默认左滑 = 返回，**严格只认左边缘
  `SWIPE_EDGE_W=44` px 内起手 + 向右（往屏幕内部）滑够 `SWIPE_BACK_DX=36` px**。
  非边缘起手 → PRESSED 时就把 `triggered` 置上，本次手势**彻底作废**
  （不是"这次不算"，是后面再滑也不算，防止误触）。
- **统一入口**：`nav.h` 的 `swipe_back_detect / swipe_back_to / swipe_back_act`。
  新屏一律用它，别再自己写 `swipe_detect(..., 40)`。
- ⚠️ 三个事件（`PRESSED` / `PRESSING` / `RELEASED`）**都要挂**同一个 cb，
  少挂 `PRESSED` 会让 `startX` 留着上一次的脏值。
- **竖向为主的手势要让给滚动**：`abs(dy) > dx` 直接 return false。
- **设置页例外**：它是多级菜单，返回要回**上一级**（`settings_menu_back` 按
  depth 判断，depth==0 才回桌面），所以走 `swipe_back_act` 而不是 `swipe_back_to`。
- **游戏例外**：贪吃蛇 / 2048 本来就用滑动当操作。做法是先跑边缘判定，
  命中就置 `g_backFired=true` 并 return；`RELEASED` 里看到 `g_backFired`
  就**不再把这次手势当一步棋**。

### I10 · 顶栏后台指示：图标排开，别再"方块 + 数字"
- **语义**（master 2026-09-25）：把真在跑的应用**图标**一字排开，
  能放几个放几个，放不下才用 `+N` 收尾（7 个 = 7 图标；8 个 = 6 图标 + `+2`）。
- ⚠️ **尺寸是被顶部空间卡出来的**（实测坐标，不是估算）：顶栏 480 宽，
  中间时间标签实测 `x 211..269`；图标行从 `x=38` 起（电池 22 + padding）。
  `18px 图标 + 3px 间距` → 7 个 = 144，行尾 `x≈182`，剩 ~29px。
  **想加数量先缩电池或供电文本**，别直接改 `TASK_MAX_ICONS`。
  改完用串口 `[StatusBar] tasks ... row x a..b` 确认 `b < 205`。
- ⚠️ **量宽度前必须 `lv_obj_update_layout()`**：flex 是**惰性**重算的，刚改完
  `HIDDEN` 标志立刻 `lv_obj_get_width()` 拿到的是上一帧的旧值 ——
  实测恒为 16，看着像"行没撑开"，其实只是没重算（白查了半小时）。
- 图标**一次性全建好再显隐**，不要在每秒的刷新里 create/delete
  （反复开 PSRAM 画布缓冲 = 碎片 + 抖动）。隐藏对象不参与 flex 布局，
  少几个时后面的会自然往前靠。
- 只在图标**真的变了**才 `icon_set_type()`（首帧用 `taskShown<0` 强制画一次），
  否则每秒白擦 7 个画布。
- 图标来源：`nav_running_list()` 给 Activity 名 → `appreg_find()` 换 `AppEntry::icon`；
  注册表里没有的（如 `touchtest`）用 `Icon::Switch` 兜底。



### I11 · 后台任务的停止标志必须**每次触发时清**，不然任务会永远空转
- **症状**：热点新闻"换了源却永远显示上一个源"，弹窗还写着"已更新"。
- **根因链（2026-09-25 实机抓出来的）**：
  1. 退出浏览器 → `BrowserScreen_close()` 把 `g_stopRequested` 置 true；
  2. 这个标志只在 `startFetch()` 和 tick 的收尾分支里清 —— 退出路径不走那两处；
  3. 于是下次 `startNews()` 唤醒常驻任务时，任务第一句
     `if (g_stopRequested) { g_taskDone = true; continue; }` **什么都不干**；
  4. tick 看到 `g_taskDone` 就照样弹"已更新"，显示的是 `g_news` 里的旧数据。
- **动作**：`startNews()` 里显式 `g_stopRequested = false; g_hasPending = false;`。
- **教训**：常驻任务 + 通知唤醒这种结构，"停止"是**一次性**的，
  每个发起方在唤醒前都要自己清干净，别指望别人。

### I12 · UTF-8 截断：只回退续字节**不够**，孤立的首字节照样是乱码
- **症状**：新闻标题末尾总带一个乱码字符（实测"…豪取亚运六<?>"，末字节 `e8`）。
- **根因**：按 cap 截断后只把尾部 `0x80~0xBF` 续字节删掉，剩下的**首字节**
  （如 `0xE8`）仍是半个字 → LVGL 画出来就是乱码 / 方框。
- **正确做法**：先回退到 lead byte，再判断 `o - start` 够不够这个字的长度，
  不够就**整个字丢掉**（首字节一起）。
- ⚠️ 反过来也不能"无条件回退"：那会把末尾**完整**汉字的续字节也删掉，
  每条都少最后一个字（这个坑 2026-09-24 踩过）。两边都要兼顾。
- 判定顺序：先找 lead byte → 算 need(2/3/4) → `(o - start) < need` 才丢。

### I13 · 豆腐块（缺字方框）：新闻这种"任意文本"要先过滤字库里没有的码位
- 我们的 `font_zh_16` 只有 3927 个常用字，新闻标题什么字都可能有 → 画成方框。
- 做法：入库前用 `lv_font_get_glyph_dsc(&font_zh_16, &dsc, cp, 0)` 探测，
  返回 false 就把该码位换成空格。就地写（多字节 → 1 字节空格，只会变短）。
- ⚠️ emoji 已经由 LVGL 补丁跳过；**中文缺字仍保留方框**是故意的，
  那是"字库里没有"的信号 —— 但面向用户的第三方文本（新闻）不该让用户看方框。

### I14 · 阻塞式 HTTP 不许出现在 UI 线程（天气）
- **症状**：点进天气页就卡死，什么都点不了。
- **根因**：`GeoIP::locate()` + HTTPS + 失败重试 2 轮，全在 `WeatherScreen_tick()`
  （UI 线程）里同步跑，最坏 2×12s ≈ 24 秒 LVGL 不转。
- **动作**：跟浏览器一致 —— **常驻后台任务 + 任务通知唤醒**，任务里只填
  `g_res` 结构，**绝不碰 LVGL**；贴标签由 tick 在 UI 线程做。
- ⚠️ 任务里用 `vTaskDelay`，不是 `delay()`。
- ⚠️ 任务栈只从 DRAM 分配 → 只建一次、永不删除（屏销毁也不删）。
- 进屏自动拉一次；10s 内的重复进屏不再联网，直接贴上次结果。

### I15 · 动画开关：`g_uiAnim` 必须定义在**匿名 namespace 外面**
- 写在 `namespace { }` 里就是内部链接：别的 TU 里 `extern` 声明能编译过，
  **链接时**才报 `undefined reference`（踩了一次，browser_screen.cpp 报错）。
- 语义（master 2026-09-25）：开关写 NVS，**重启才生效**，默认开。
  运行时不改 `g_uiAnim` —— 否则"已经在飞的动画"和"新动画"状态不一致。
  设置项右侧文字只在"存储值 ≠ 当前生效值"时才显示"· 重启生效"，不然太吵。

### I16 · 返回要有动画，但**不能边动画边删屏**
- `nav_back_home()` 是「无动画 lv_scr_load → 立刻 lv_obj_del 旧屏」，
  那是被逼的：动画期间旧屏仍参与渲染，删了必崩。
- 要动画就用 `nav_back_home_anim()`：**先 `lv_scr_load_anim` 切走，
  280ms 后**（动画早就跑完）再用一次性 lv_timer 销毁旧屏。
- 二级菜单是**同一个屏换数据源**，没有第二个 screen 可切 → 用容器的
  x 位移动画（进下级从右、回上级从左），基准位置别忘了是 x=12。

### I17 · 手势策略（2026-09-25 改版）
- **左右滑动退出 = 全页通用**，任意起手点都行（`swipe_back_to_any`），
  触发距离放大到 48px 防误触。
- **上下滑动退出 = 按页面开关**：默认开，浏览器这种要滚动的页面传
  `allowVertical=false`。竖向只认**上下边缘 44px 内起手**，中间起手让给滚动。
- ⚠️ 游戏（贪吃蛇/2048）、画板、触摸测试**必须继续用边缘版**
  `swipe_back_detect` —— 它们本来就把滑动当操作。
- 设置页返回的是**上一级**，走 `swipe_back_act_any`。

### I18 · URL 拼进固定 buf 会被 snprintf 悄悄截断 → 服务端 400
- **症状**：天气加了 `hourly` 之后设备端一直 `HTTP 400`，
  而**同一个 URL 在 PC 上 200**（很容易误判成"对方接口不接受我的参数"）。
- **根因**：`makeWeatherUrl()` 的 `char buf[512]`，完整 URL 实测 **594 字节** →
  `snprintf` 静默截断，服务端收到半个参数（`forecast_hours=2`）直接 400。
- **动作**：buf 放大到 768，并在注释里写清楚实测长度。
- **教训**：设备端 400 / PC 端 200，**先怀疑 URL 被截断**，别急着改参数。
  任何"拼长 URL"的地方都要留 >= 实际长度 1.3 倍的余量。

### I19 · 一串 `g_xxx was not declared` 的**真正**根因常是"声明顺序"，不是 namespace
- **症状**：改完 weather_screen.cpp 后冒出
  `'g_hourLab' / 'g_dayDate' / 'g_dayTmp' … was not declared in this scope` 一长串，
  很容易误判成"匿名 namespace 里的符号外面看不见"。
- **❌ 误判**：我当时就是这么写的，还据此把一批变量搬出了 namespace —— 白改。
  反证很硬：nav.cpp 的 `nav_open()` 就在 `}  // namespace` **之后**，照样调用
  namespace 内的 `find()`，一直编译得好好的。
  匿名 namespace 等价于 `namespace unique {…} + using namespace unique;`，
  **关闭之后仍然可以用非限定名访问**。
- **✅ 真因**：`constexpr int DAY_N` 被挪到了 `struct DayFc` 前面（第 66 行），
  可 `lv_obj_t* g_dayDate[DAY_N]` 在第 55 行就用到它 → `DAY_N` 未声明 →
  这一行数组声明**整条失效** → 后面每个用到它的地方都报"未声明"。
- **教训**：看到"一串变量未声明"，**先去看这些声明所在行的依赖**（数组长度常量、
  类型名）是不是在后面才定义；别急着怪 namespace / 链接性。
- 顺带留下的规矩仍然成立：要被别的 .cpp 链接的符号（create/tick/fetchNow）
  必须有外部链接（I15 的 `g_uiAnim` 是同一条规矩的另一种翻车）。

### I20 · LVGL 8.3 没有 `lv_obj_set_scroll_dir()`（8.4 才有）
- 想让"逐时天气"横向滚，调它直接 `error: 'LV_SCROLL_DIR_HOR' was not declared`。
- 而且就算有，在**竖向滚动的父容器里**嵌一个横向滚动容器，手指竖滑会先被
  子容器吃掉，下面的 7 日列表就滑不动了。
- 本项目选的做法：逐时那 24 格用 `LV_FLEX_FLOW_ROW_WRAP` 排成 8 列 × 3 行，
  整体跟着页面竖滑 —— 一次能看全，也不用跟手势打架。

### I21 · "编译成功"可能是陈旧 .o 的假阳性
- **症状**：先看到 `[SUCCESS]`，下一轮改别的才暴露出上一次根本没编的错。
- **动作**：改完必须核对 `.o` 与源文件 mtime（`FRESH` 才算过），
  并且 `grep error: build_sdkdef.log` 看计数。
- ⚠️ 改 `lv_conf.h` 后还要删 `.wb_build/.../lib51c` 逼 LVGL 重编（老规矩，见 C 章）。

### I22 · 回桌面 ≠ 退出：`nav_back_home()` 曾经把所有后台都清了
- **症状**（master 原话）：「从应用切入后台之后，我无论点不点结束，它都会帮我结束掉」。
- **根因**：`nav_back_home()` / `nav_back_home_anim()` 里调
  `nav_release_all_except(nav_launcher)` —— **回桌面那一刻就把 launcher 以外的
  所有 Activity 全 release 了**，不只是刚离开的那个。所以后台管理里点不点"结束"
  都一样：应用早没了。串口实锤（`navlist` / `back` 命令）：
  `running=2 (clock, weather)` → `back` → `release clock` + `release weather` → `running=0`。
- **改法**：回桌面 = 切后台，只 `lv_scr_load(nav_launcher)`，**绝不销毁**；
  内存压力交给 `nav_open()` 之前的 `trimBackground()` 按 LRU 兜底
  （后台 > 4 个、或 DRAM < 48KB 时，回收最久没进前台的那个；
   前台 / 正要开的 / canRelease 守不过的一律不动）。
- 仍然**独占**的场景：Launcher 进浏览器照旧
  `nav_release_all_except(nav_launcher, nav_browser)` —— 它太重，别跟别人挤。
- 诊断命令：`navlist`（列还在内存里的 Activity）、`back`（走真实返回桌面路径）。

## J. 工具 / 环境（Windows 特供）

### J1 · ⚠️ Edit 工具会报成功但不落盘
本项目 **CRLF 文件上已踩 6+ 次**。
**每次编辑后必须 grep 回读核对**；不确定就用托管 python 做字符串替换 + 立即校验。

### J2 · bash shim 是坏的
`ls` / `head` / `grep` / `tail` / `dirname` 都 not found，`rc=127` **不代表命令失败**。
→ 用 PowerShell 或托管 python 代替。
⚠️ `build_run.py 2>&1 | tail -40` 会因为 `tail` 不存在而"失败"。

### J3 · 写文件别用反引号
会被命令替换吃掉。

### J4 · PowerShell 常不回显 stdout
→ 用 `*>` 重定向到文件（UTF-16），再用托管 python 解码读取。

### J5 · 生成/传参绕开 shell
字符集或参数里有中文/特殊字符 → 写文件 + Node `spawnSync` 传 argv 数组。

---

## K · 分段渲染（2026-09-25）

### K1 · MAX_WIDGETS 不是内存保护，是"直接砍内容"
它是**硬闸门**：第 201 个 widget 之后的内容全部丢弃。长页面（必应/360 中文搜索）
后半截凭空消失，看着像"加载失败"。改成**分段渲染**后不再是问题：
排版/摊平/去垃圾只做一次（`s_preparedRoot` 按 root 指针记），
先干跑数出瓦片总数，再只铺 `[segStart, segStart+segCount)`。
翻段 = 拿同一棵布局树重铺，实测 19~73 ms，不联网、不重新解析。
⚠️ 布局树渲染完**不再释放**，只有 `startFetch()` / 退出浏览器（freeLayoutTree）才释放。

### K2 · if / else-if 链会把同一个节点数两遍
`seg_take_tile()` 放在**分支条件**里：胶囊分支判定为 false 时会**掉到下一个
else-if** 再判一次 → 同一个节点领了两块瓦片。
实测干跑把每个胶囊都数了两遍（154 vs 真实 138），段数全错。
→ `seg_take_tile(node)` 按**节点**记结果，同一节点重复调用直接返回上次的值。

### K3 · 翻段按钮在 g_content 里 → 不能在自己的回调里清内容
和链接跳转同一个坑：回调里 `contentReset()` 会把正在派发事件的按钮删掉。
→ 回调只置 `g_segPending`，真正翻段在 `BrowserScreen_tick()` 里做。

### K4 · 摊平/缩放只能做一次
`layout_flatten_tree` / `layout_scale_tree` / `layout_drop_junk` 都会**改写树**，
翻段时重复跑会串味。用 `s_preparedRoot` 挡住。
⚠️ 树被释放后必须 `layout_forget_prepare()`，否则新树可能复用同一块地址，
被误判成"已准备"而跳过摊平。

### K5 · 匿名 namespace 里别重复前向声明
`static void foo();` + 后面的 `static void foo() {...}` 会被当成**两个重载**，
调用处报 `call of overloaded 'foo()' is ambiguous`。要前向声明就只留一份。

---

## L · SD 卡页面缓存（2026-09-25）

### L1 · ⛔ SD.mkdir 不递归
`SDCard::writeFile` 只 mkdir **直接父目录**。写 `/gt/pages/xxx.html` 时父目录
`/gt/pages` 建不出来（`/gt` 都不存在）→ `SD.open` 返回空 → 静默写失败。
→ 网页缓存平铺在 `/gt/p%08x.html`。

### L2 · ⛔ 缓存时间戳只记 millis() → 重启后全部判成过期
`millis()` 重启归零，`millis() - ts` 是巨大的无符号数，每一份缓存都被判过期
（实测：刚写进卡、重启再打开，照样走联网）。
→ 头部同时记 `EP:<time(nullptr)>`，有校时就按真实时间算 TTL；
没校时才退回 millis 那条路（`ts > millis()` = 上次开机写的，直接认）。

### L3 · String 拼二进制 HTML 要用 concat(ptr, len)
`String((const char*)data)` 遇到 NUL 会截断；`out.concat((const char*)d, len)` 才安全。

### L4 · ⛔⛔ String 读文件遇 0x00 会截断（本次最贵的坑）
`SDCard::readFile` 以前写的是 `out += buf`（`buf` 是 `char[256]`）。
`String::operator+=(const char*)` 走 **strlen** —— 文件里每出现一个 `0x00`，
它所在的那一块剩下的字节**全部丢掉**。
实测：131072 B 的测试文件只读回 71892 B，内容从头就错位。

网页 HTML 的内联 JS 里就带 `0x00`，于是缓存读回来是**错位的字节流**，
lexbor 把 JS 当成标签解析 —— 表面上看到的症状是：
- 页面上冒出一坨脚本源码（`[Flat] 8 ];window.onunhandledrejection=...`）；
- 出现假标签（`<bodyzoomparams.browserwidth&&_w.innerheight<...>`）；
- 瓦片数从 62 掉到 32，整页内容缩水。

**看着像"垃圾过滤没做"，其实是 SD 读坏了。** 加 junk 过滤只会把真问题盖住。
→ 用 `out.concat((const char*)buf, n)` 按长度拷，不依赖 NUL 结尾；
  顺便 `reserve(f.size()+1)` 免得每块都重新分配。
→ 排查这类问题用 `SDCard::selfTest()`（串口 `sdtest [kb]`）：
  写一段已知字节模式再读回逐字节比对，一次分清是 SD 层还是 HTML 层。

### L5 · SD 只在"已挂载"时参与
挂载要占用与 LCD 共用的那条 SPI，**绝不在浏览过程中偷偷 mount**（串口 `sd` 才挂）。
没挂载一律静默跳过，退回 PSRAM 2 槽缓存 / LittleFS 下载。

---

## M · 网页图片：缩略图 / 全屏 / 另存（2026-09-25）

链路：`<img>` → 后台任务下原始字节（`img_dsc`）→ **渲染前**在 UI 线程重采样成
小图（`img_thumb`，默认 96px）→ 页面上一小块 → 点开全屏大图 → 另存。

### M1 · ⛔⛔ 图片必须在"解析之前"抓，顺序不能换
**症状**：页面文字正常，但一张图都没有。串口里每张图都是
`[Img] miss rc=-1`，前面还有一行 `[E][WiFiGeneric.cpp:1583] hostByName(): DNS Failed`。

**根因**：解析 + 建布局树会把**内部 DRAM** 从 ~94KB 吃到只剩几百字节，
之后 DNS 解析发不出去。跟 TLS 一点关系都没有 —— 报错在 DNS 那一步就断了。

**正确顺序**（`fetch_task`）：
```
下 HTML → 扫原始 HTML 里的 <img> 地址 → 趁 DRAM 富余把图抓完
        → 解析 + 建布局树 → layout_assign_images() 按 URL 认领回节点
```
代价是要在原始 HTML 上做一遍朴素扫描（`scanImageUrls`），
属性优先级必须跟 `dom_renderer` 一致（`src` > `data-src` > `data-original`），
否则两边取到不同 URL，认领时对不上。

### M2 · ⛔⛔ LVGL 内存池大小决定 PNG 能不能解
`lodepng` 的分配器是 `lodepng_malloc() { return lv_mem_alloc(size); }`
（`lvgl/src/extra/libs/png/lodepng.c`）—— 走的是 **LVGL 自己的池**，不是 PSRAM 直通。
池原本 `LV_MEM_SIZE = 128KB`，而一张 300x200 的 PNG 光解码输出就要
300*200*4 = **240KB** → `lv_mem_alloc` 必失败 → PNG 解码器 `open_cb` 返回 INV。
**已改成 768KB**（池本身就在 PSRAM，多占 640KB，PSRAM 还有 6MB+）。

### M3 · ⛔⛔ 内建解码器会"递假像素"，而且不报错
`lv_img_decoder_open` 是**依次问** SJPG → PNG → 内建。
内建解码器的受理范围是 `CF_BUILT_IN_FIRST(TRUE_COLOR=4) .. CF_BUILT_IN_LAST(ALPHA_8BIT)`。
如果源 dsc 的 `header.cf` 填了 `LV_IMG_CF_TRUE_COLOR_ALPHA(5)`，那么当 PNG
解码器失败（见 M2）时，**内建会接管**，而它对 VARIABLE 源的做法是：
```c
dsc->img_data = img_dsc->data;   /* 把压缩的 PNG 原文当像素交出来 */
```
于是我们按 300x200x3 去读一个 8KB 的缓冲区 → 屏幕上**一坨彩色乱码**，
而 `dec.header.w/h` 还是我们自己填的 300x200，日志看着一切正常。

**修**：源 dsc 的 PNG 用 `LV_IMG_CF_RAW_ALPHA(2)`（这本来就是 LVGL 对 png
**文件**源的官方写法），2 < 4 落在内建受理范围外 —— 只有 PNG 解码器能认领，
失败就是干净地失败。另加一条兜底：`img_data == dsc->data` 一律当"没解出来"。

### M4 · JPEG 解码降采样走 `lv_img_header_t.reserved`
tjpgd 的 `jd_decomp()` 支持 1/1、1/2、1/4、1/8（`tjpgdcnf.h` 里 `JD_USE_SCALE=1`），
但 LVGL 固定传 0。网页图动辄上千像素宽，原图 `w*h*3` 的 RGB888 中间缓冲直接吃穿 PSRAM。
补丁把档位放在 **`header.reserved`（2 bit，正好 0~3）随图携带**，
`jd_decomp(..., scale)` 且 `header->w/h` 一并右移。
⚠️ **别用全局变量存档位**：解码是懒加载的，LVGL 图片缓存淘汰后会重新 open，
那时全局变量早被别的图改过了 —— 会缩错。
存档脚本：`tools/patch_lvgl_jpeg_scale.py`。同脚本还把 `is_jpg()` 从
"必须有 JFIF APP0"放宽到 `FF D8 FF`——原实现会拒掉 Exif / 无 APP0 的一大半 JPEG。

### M5 · 缩略图瓦片会被导航胶囊挤出第 1 段
163 首页的导航条摊平出 ~58 个胶囊，`PAGE_SEG_TILES=60` 时前 58 块全被吃光，
图片节点排在 60 名开外 → **第 1 段一块缩略图都铺不出来**，现象是"页面上根本找不到图"。
已把 `PAGE_SEG_TILES` 提到 **80**。换页面如果还是看不到图，先跑 `imgscan`
看有几张、再翻段。

### M6 · 文本节点用裸 malloc = DRAM 黑洞
`dom_renderer` 建文本节点时曾经是 `malloc(trimmed_len+1)`。
`CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=4096` 会把 ≤4KB 的 malloc **一律塞进内部 DRAM**，
而文本节点是布局树里数量最多的东西（一页几百个）→ 解析一页 DRAM 掉几十 KB。
它兄弟（`safe_strdup` / `safe_strndup` / 元素文本）早就走 `tb_alloc` 了，只有这条漏了。
改 `tb_alloc` 的同时补了判空 —— 原代码不判空就 `memcpy`，DRAM 见底时就是崩溃。

### M7 · 图片重采样为什么不用 `lv_img_set_zoom`
两条都踩过：
1. JPEG 在 LVGL 里是 RAW + 逐行 `read_line`（`img_data` 为 NULL）。LVGL 缩放时
   拿**每一行**当整张图单独变换，画出来上下错位；
2. zoom 是最近邻，缩到 1/5 就是马赛克 —— 而我们要的是"糊但看得清"。
所以自己在 `tb_image_resample()` 里把像素抠出来做**区域平均**（box filter）：
· JPEG → `lv_img_decoder_read_line` 逐行取（出来已经是 RGB565，2 字节/像素）；
· PNG  → 直接从 `dec.img_data` 取（RGB565+alpha，3 字节/像素）。
全屏大图 = 同一函数换个 box。

### M8 · 诊断命令
`img`（开关）/ `thumb <px>`（缩略图尺寸）/ `imgscan`（本页几个 `<img>`、几个取到地址）/
`imgview <n>` / `imgdl <n>` / `imgtest <url>`（单张图下载+解码+重采样全链路）。

---

## 附：一句话红线

- **见 `stack overflow in task loopTask` → 是栈不是堆**，去加 `ARDUINO_LOOP_STACK_SIZE`，别去加 PSRAM
- **遍历树：兄弟用迭代，父子才递归**（写反 = 递归深度等于节点总数）
- **`layout_node_destroy` 不许加深**：截断 = 泄漏
- **链接不一定是 URL**：点之前先过 `isPseudoUrl()`

1. **Flash=DIO + PSRAM=OPI**，两条同时成立，缺一开不了机。
2. **别配 `flash_size=16MB` / `partitions_16MB.csv`** → bootloop 且无 panic。
3. **PCLK=10MHz**，别改回 16。
4. **编译后核对 `.o` 的 mtime**，退出码不可信。
5. **别删 `.pio`**，只删 `.wb_build`。
6. **运行时禁 `heap_caps_get_largest_free_block()` / `get_info()`** → wdt。
7. **先切屏再删屏**；`lv_timer` 要自己注销。
8. **改排版前先看 `12`/`14` 的 F 段**，改字体前先看 `09`。
